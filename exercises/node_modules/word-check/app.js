
var wordArray = require('./words.json')

function wordChecker(str){

    if (wordArray.indexOf(str) > -1) {

        console.log(str +" is in the list and inappropriate");

    } else {

        console.log(str + "is not in the list");

    }
}

module.exports = wordChecker;